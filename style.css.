body {
    font-family: Arial, sans-serif;
    background-color: #000;
    color: #d4eaf7;
    text-align: center;
}
header {
    background: #111;
    padding: 20px;
    font-size: 24px;
}
nav a {
    color: #d4eaf7;
    margin: 10px;
    text-decoration: none;
}
.content {
    padding: 20px;
    background: #222;
    border-radius: 5px;
    max-width: 800px;
    margin: auto;
}
button {
    background: #d4eaf7;
    color: #000;
    padding: 10px;
    border: none;
    cursor: pointer;
}
